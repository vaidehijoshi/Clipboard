<<<<<<< HEAD
<h2>Works Well With</h2>
<% if @student.buddies.any? %>
  <ul>
    <% @student.buddyships.each do |buddyship| %>
      <% course_section = CourseSection.find(buddyship.course_section_id) %>
      <% buddy = Student.find(buddyship.buddy_id) %>
      <li>
      <%= link_to buddy.full_name, student_path(buddy) %> | 
      <%= link_to 'no longer buddies', teacher_class_buddyship_path(course_section.teacher, course_section, buddyship), method: 'DELETE' , data: {confirm: "Are you sure?"} %>
      </li>
    <% end %>
  </ul>
<% else %>
<p><%= @student.first_name %> has no work buddies.</p>
<% end %>

<h2>Works Badly With</h2>
<% if @student.enemies.any? %>
  <ul>
    <% @student.enemies.each do |enemy| %>
      <li><%= link_to enemy.full_name, student_path(enemy) %></li>
    <% end %>
  </ul>
<% else %>
<p><%= @student.first_name %> has no work enemies.</p>
<% end %>
=======
>>>>>>> master
